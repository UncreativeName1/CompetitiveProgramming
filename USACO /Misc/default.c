/*
ID: beetwan1
LANG: C
PROG: [TASK NAME]
*/
#include <stdio.h>
#include <stdlib.h>

void main () {
    FILE *fin  = fopen ("[TASK NAME].in", "r");
    FILE *fout = fopen ("[TASK NAME].out", "w");
    exit (0);
}